<h1>Dashboard page</h1>

<ul>
  <% @workspaces.each do |workspace| %>
    <li>
      <%= link_to workspace.name, workspace_path(wid: workspace.unique_id, name: workspace.name.parameterize)%>
    </li>
  <% end -%>
  <li><%= link_to "New Workspace", new_workspace_path %></li>
</ul>
